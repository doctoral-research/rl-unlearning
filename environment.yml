name: rl-unlearning
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults

dependencies:
  - python=3.12
  - pytorch>=2.0.0
  - pytorch-cuda=12.4
  - numpy>=1.24.0
  - scipy>=1.11.0
  - pip>=24.0
  - pip:
      - -r requirements.txt
      - -e ".[dev]"
